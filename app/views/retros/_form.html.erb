<%= form_with(model: retro, local: true) do |form| %>
  <% if retro.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(retro.errors.count, "error") %> prohibited this retro from being saved:</h2>

      <ul>
      <% retro.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :title %>
    <%= form.text_field :title, id: :retro_title %>
  </div>

  <div class="field">
    <%= form.label :verdict %>
    <%= form.text_field :verdict, id: :retro_verdict %>
  </div>

  <div class="field">
    <%= form.label :completed %>
    <%= form.check_box :completed, id: :retro_completed %>
  </div>

  <div class="field">
    <%= form.label :completed_date %>
    <%= form.datetime_select :completed_date, id: :retro_completed_date %>
  </div>

  <div class="field">
    <%= form.label :user_id %>
    <%= form.text_field :user_id, id: :retro_user_id %>
  </div>

  <div class="field">
    <%= form.label :project_id %>
    <%= form.text_field :project_id, id: :retro_project_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
